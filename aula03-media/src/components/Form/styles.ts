import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    gap: 12,
  },
  containerDiv: {
    alignItems: "center",
    gap: 6,
    marginTop: 20,
  },
  textTitle: {
    position: "absolute",
    top: -40,
    color: "#A1A1AA",
    fontSize: 16,
    fontWeight: "bold",
  },

});
